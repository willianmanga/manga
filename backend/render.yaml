services:
  - type: web
    name: mangarx-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
